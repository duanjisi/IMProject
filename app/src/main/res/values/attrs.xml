<?xml version="1.0" encoding="utf-8"?>
<resources>
    <attr name="rightPadding" format="dimension" />

    <declare-styleable name="SlidingMenu">
        <attr name="rightPadding" />
    </declare-styleable>
    <declare-styleable name="GifView">
        <attr name="gif" format="reference" />
        <attr name="paused" format="boolean" />
    </declare-styleable>
    <declare-styleable name="CustomTheme">
        <attr name="gifViewStyle" format="reference" />
    </declare-styleable>
    <declare-styleable name="TopNavigationBar">
        <attr name="topbarTitle" format="string" />
        <attr name="topbarIcon" format="reference" />
        <attr name="showArrowIm" format="boolean" />
        <attr name="showSearchBar" format="boolean" />
        <attr name="isSearchBarEdit" format="boolean" />
        <attr name="hideLeftImBtn" format="boolean" />
        <attr name="keyboardHasSearchBtn" format="boolean" />
        <attr name="isResponseEditor" format="boolean" />
        <attr name="leftImBtnDrawable" format="reference" />
        <attr name="rightImBtnDrawable" format="reference" />
        <attr name="rightImBtnSecondDrawable" format="reference" />
        <attr name="rightBtnStatus">
            <enum name="normal" value="0" />
            <enum name="tv" value="1" />
            <enum name="imBtn" value="2" />
            <enum name="both" value="3" />
        </attr>
        <attr name="leftBtnStatus">
            <enum name="normal" value="0" />
            <enum name="tv" value="1" />
            <enum name="imBtn" value="2" />
            <enum name="both" value="3" />
        </attr>
        <attr name="rightText" format="string" />
        <attr name="leftText" format="string" />
    </declare-styleable>
    <declare-styleable name="EaseContactList">
        <attr name="ctsListPrimaryTextColor" format="color" />
        <attr name="ctsListPrimaryTextSize" format="dimension" />
        <attr name="ctsListShowSiderBar" format="boolean" />
        <attr name="ctsListInitialLetterBg" format="reference|color" />
        <attr name="ctsListInitialLetterColor" format="color" />
    </declare-styleable>

    <declare-styleable name="ViewPagerIndicator">

        <!-- Style of the circle indicator. -->
        <attr name="vpiCirclePageIndicatorStyle" format="reference" />
        <!-- Style of the icon indicator's views. -->
        <attr name="vpiIconPageIndicatorStyle" format="reference" />
        <!-- Style of the line indicator. -->
        <attr name="vpiLinePageIndicatorStyle" format="reference" />
        <!-- Style of the title indicator. -->
        <attr name="vpiTitlePageIndicatorStyle" format="reference" />
        <!-- Style of the tab indicator's tabs. -->
        <attr name="vpiTabPageIndicatorStyle" format="reference" />
        <!-- Style of the underline indicator. -->
        <attr name="vpiUnderlinePageIndicatorStyle" format="reference" />
    </declare-styleable>

    <attr name="centered" format="boolean" />
    <attr name="selectedColor" format="color" />
    <attr name="strokeWidth" format="dimension" />
    <attr name="unselectedColor" format="color" />

    <declare-styleable name="CirclePageIndicator">

        <!-- Whether or not the indicators should be centered. -->
        <attr name="centered" />
        <!-- Color of the filled circle that represents the current page. -->
        <attr name="fillColor" format="color" />
        <!-- Color of the filled circles that represents pages. -->
        <attr name="pageColor" format="color" />
        <!-- Orientation of the indicator. -->
        <attr name="android:orientation" />
        <!-- Radius of the circles. This is also the spacing between circles. -->
        <attr name="radius" format="dimension" />
        <!-- Whether or not the selected indicator snaps to the circles. -->
        <attr name="snap" format="boolean" />
        <!-- Color of the open circles. -->
        <attr name="strokeColor" format="color" />
        <!-- Width of the stroke used to draw the circles. -->
        <attr name="strokeWidth" />
        <!-- View background -->
        <attr name="android:background" />
    </declare-styleable>

    <declare-styleable name="CircleImageView">
        <attr name="civ_border_width" format="dimension" />
        <attr name="civ_border_color" format="color" />
        <attr name="civ_border_overlay" format="boolean" />
        <attr name="civ_fill_color" format="color" />
    </declare-styleable>

    <!--图片截取取景框-->
    <declare-styleable name="ClipImageView">
        <attr name="civHeight" format="integer" />
        <attr name="civWidth" format="integer" />
        <attr name="civTipText" format="string" />
        <attr name="civTipTextSize" format="dimension" />
        <attr name="civMaskColor" format="color" />
        <attr name="civClipPadding" format="dimension" />
        <attr name="civClipRoundCorner" format="dimension|reference" />
        <attr name="civClipCircle" format="boolean" />
    </declare-styleable>

    <declare-styleable name="RoundImageView">
        <attr name="round_borderRadius" format="dimension" />
        <attr name="round_type" format="integer" />
    </declare-styleable>

    <declare-styleable name="PraiseListView">
        <attr name="item_color" format="color" />
        <attr name="item_selector_color" format="color" />
    </declare-styleable>

    <declare-styleable name="ExpandTextView">
        <attr name="showLines" format="integer" />
    </declare-styleable>

    <declare-styleable name="SlideListView">
        <attr name="slideAnimationTime" format="integer" />
        <attr name="slideMode" format="enum">
            <enum name="none" value="0" />
            <enum name="left" value="1" />
            <enum name="right" value="2" />
            <enum name="both" value="3" />
        </attr>
        <attr name="slideLeftAction" format="enum">
            <enum name="scroll" value="0" />
            <enum name="reveal" value="1" />
        </attr>
        <attr name="slideRightAction" format="enum">
            <enum name="scroll" value="0" />
            <enum name="reveal" value="1" />
        </attr>
    </declare-styleable>

    <item name="slide_id_front_view" type="id" />
    <item name="slide_id_left_back_view" type="id" />
    <item name="slide_id_right_back_view" type="id" />
</resources>